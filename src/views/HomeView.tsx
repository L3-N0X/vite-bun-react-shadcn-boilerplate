export function HomeView() {
    return (
        <div className="flex items-center justify-center flex-col gap-4">
            <h2 className="text-4xl">Hello!</h2>
            <p className="text-base">from bun + react + vite + shadcn/ui</p>
        </div>
    )
}
